{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "_comment": "the guy in the CRUD video put the start script in idk if we need it",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0",
    "firebase-admin": "^13.4.0"
  }
}
